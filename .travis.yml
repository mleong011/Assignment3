dist: xenial   # required for Python >= 3.7
language: python
python:
  - "3.7"
install:
  -pip install -r requirements/dev.txt

  # install dependencies
install:
  - pip install codecov

# run scripts
script:
  - coverage erase
  - coverage run nasapod.py && coverage html

after_success:
  - codecov
